<div class="container" id="<%= dom_id cost_benefit_analysis %>">

  <div data-controller="cost-benefit-analysis">
    <div data-cost-benefit-analysis-target="accountId" data-value="<%= @account.id %>" >
    </div>
    <div data-cost-benefit-analysis-target="exerciseId" data-value="<%= cost_benefit_analysis.id %>" >
    </div>
    <div data-cost-benefit-analysis-target="exerciseClass" data-value="<%= cost_benefit_analysis.class.name %>" >
    </div>
    <div data-cost-benefit-analysis-target="totalPossiblePoints" data-value="<%= cost_benefit_analysis.point_system.total_possible_points unless cost_benefit_analysis.point_system.blank? %>" >
    </div>
    <div data-cost-benefit-analysis-target="pointsDeductionForMistake" data-value="<%= cost_benefit_analysis.point_system.points_deduction_for_mistake unless cost_benefit_analysis.point_system.blank? %>" >
    </div>


      <p class="text-center">
          <%= raw cost_benefit_analysis.content %>
      </p>
      <div class="mb-3 border rounded p-3">
        <p>
          <strong>Anleitung: </strong>
        </p>
        <p>
          Überlege dir die Nutzen und Schäden und weise ihnen einen Wert zu.
        </p>
        <p>
          1. Pro Linie kannst du einen Eintrag machen.
        </p>
        <p>
          2. Mach am Ende einen Doppelpunkt ":" und schreib den Wert hin.
        </p>
        <p>
          <strong> Zum Beispiel: </strong><br>
          Mehr Gesundheit dank Krankenkassen : 12
        </p>
        <p>
          Mehr Frustration wegen höheren Steuern : -5
        </p>
        <p>
          3. Neue Linie für den nächsten Eintrag machen.
        </p>
        <p>
          4. Wenn du fertig bist, auf "Antwort speichern" drücken.
        </p>
      </div>
      <div class="mb-3">
        <label for="" class="form-label">Nutzen</label>
        <textarea class="form-control" data-cost-benefit-analysis-target="benefitInput" type="text" rows="5" placeholder="Nutzen 1 : 5 &#10;Nutzen 2 : 3 &#10;Nutzen 3 : 8 &#10;Nutzen 4 : 12"></textarea>
      </div>
      <div class="mb-3">
        <label for="" class="form-label">Kosten</label>
        <textarea class="form-control" data-cost-benefit-analysis-target="costInput" type="text" rows="5" placeholder="Schaden 1 : -2 &#10;Schaden 2 : -14 &#10;Schaden 3 : -6 &#10;Schaden 4 : -9"></textarea>
      </div>

      <table class="table table-striped" data-cost-benefit-analysis-target="fullTable">
        <thead>
          <tr>
            <th scope="col">Nutzen / Kosten</th>
            <th scope="col">Wert</th>
          </tr>
        </thead>
        <tbody data-cost-benefit-analysis-target="tableOutput">

        </tbody>
      </table>


      <p>
        <button data-action="click->cost-benefit-analysis#save" class="btn btn-primary">
          <%= t('save_answer') %>
        </button>
      </p>

    </div>

    <span data-quiz-target="output">
    </span>
  </div>

</div>
