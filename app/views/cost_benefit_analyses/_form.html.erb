<%= form_with(model: cost_benefit_analysis) do |form| %>
  <% if cost_benefit_analysis.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cost_benefit_analysis.errors.count, "error") %> prohibited this cost_benefit_analysis from being saved:</h2>

      <ul>
        <% cost_benefit_analysis.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :slide_id, style: "display: block" %>
    <%= form.text_field :slide_id %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :cost, style: "display: block" %>
    <%= form.text_area :cost %>
  </div>

  <div>
    <%= form.label :benefit, style: "display: block" %>
    <%= form.text_area :benefit %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
