<div class="card">
  <div class="card-body">
    <h5 class="card-title"><%= course.name %></h5>
    <h6 class="card-subtitle mb-2 text-muted">Card subtitle</h6>
    <p class="card-text">
      <%= course.description %>
    </p>
    <%= link_to "Loslegen!", course_path(course), class: "primary-button" %>
  </div>
</div>
