<%= form_with(model: quiz_question) do |form| %>
  <% if quiz_question.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quiz_question.errors.count, "error") %> prohibited this quiz_question from being saved:</h2>

      <ul>
        <% quiz_question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :correct_answer, style: "display: block" %>
    <%= form.select :correct_answer, QuizQuestion.correct_answer_options %>
  </div>

  <div>
    <%= form.label :quiz_id, style: "display: block" %>
    <%= form.select :quiz_id, Quiz.all.map {|quiz| [quiz.content, quiz.id]}, {include_blank: true}, {class: "form-select"}  %>

  </div>



  <div>
    <%= form.submit %>
  </div>
<% end %>
