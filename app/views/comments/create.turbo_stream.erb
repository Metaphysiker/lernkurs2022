<%= turbo_stream.append "comments_of_slide_id_#{@comment.commentable_id}" do %>
  <%= render "comment", comment: @comment %>
<% end %>
<%= turbo_stream.replace "#{dom_id(Comment.new)}_form" do %>
  <%= render "form", comment: Comment.new(content: "") %>
<% end %>
