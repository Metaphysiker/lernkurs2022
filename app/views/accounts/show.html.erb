<%= render "layouts/navigation" %>

<div class="container">
<p style="color: green"><%= notice %></p>

<%= render @account %>

<div>
  <%= link_to t("helpers.submit.update", model: Account.model_name.human), edit_account_path(@account) %>

  <%= button_to t("delete_account"), @account, method: :delete, class: "btn btn-danger" %>
</div>
</div>
<%= render "layouts/footer" %>
