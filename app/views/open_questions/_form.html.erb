<%= form_with(model: open_question) do |form| %>
  <% if open_question.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(open_question.errors.count, "error") %> prohibited this open_question from being saved:</h2>

      <ul>
        <% open_question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :slide_id, style: "display: block" %>
    <%= form.text_field :slide_id %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :correct_answer, style: "display: block" %>
    <%= form.text_area :correct_answer %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
